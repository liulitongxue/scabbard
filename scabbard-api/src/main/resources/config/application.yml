server:
  port: 11302
netty:
  server:
    port: 11303

spring:
  application:
    name: scabbard-api
  profiles:
    active: '@spring.profiles.active@'
  mvc:
    static-path-pattern: /**
  resources:
    static-locations: classpath:/public/,classpath:/static/