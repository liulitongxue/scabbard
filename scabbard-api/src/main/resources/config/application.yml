server:
  port: 11302
netty:
  server:
    port:
      websocket: 11304
      tcp: 11306
      udp: 11308

spring:
  application:
    name: scabbard-api
  profiles:
    active: '@spring.profiles.active@'
  mvc:
    static-path-pattern: /**
  resources:
    static-locations: classpath:/public/,classpath:/static/